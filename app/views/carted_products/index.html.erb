<h1> Check Out Page </h1>

<% @carted_products.each do |carted_product| %>
  <h2> <%= carted_product.bee.name %> </h2>  
  <h2> <%= carted_product.bee.price %> </h2>  
  <h2> <%= carted_product.quantity %> </h2>  
  <% end %>

<%= link_to "Order now", "/orders", method: :post %>
